server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:~/bancodesanguedb;DB_CLOSE_ON_EXIT=FALSE
    username: fiap
    password: fiap
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update